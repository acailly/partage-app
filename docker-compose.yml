services:

  pocketbase:
    image: pocketbase
    container_name: pocketbase
    env_file:
     - ./.env
    ports:
      - '8080:8080'
    volumes:
      - ./pb_data:/pb/pb_data
      - ./pb_hooks:/pb/pb_hooks
      - ./pb_public:/pb/pb_public
